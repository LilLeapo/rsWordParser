# 协议随机门的最小化回归

`table-revision-10201016167453558198.json` 来自 TEST-07 的
`corpus/synthetic/m6-chart__070.docx`、seed `10201016167453558198`，40 步最小化为 26 步。
每项是 `[EditOp JSON, EditContext JSON]`；第 20 次请求后保存并重开，节点编号因此与该边界绑定。
最后一次 reject 必须成功，结果为四列网格与三行四列，不允许靠新增拒绝掩盖回归。
`random_ops.rs` 同时沿原最小化复放路径验证两视图与周期保存。

这是自有编辑请求记录，不是 TS oracle；不得修改请求来适应引擎回归。
网格修复已获过渡实现授权，spec/18 7.4 的措辞仍待负责人裁定。
