# CASE-COMBINED-01 C01 checkpoint

## Word UI action

- Opened `CASE-COMBINED-01-C01.docx` in Microsoft Word and fixed the four H4 placeholder paragraphs.
- `Cmd+Option+4` did not apply Heading 4 in the observed Word UI. Each H4 paragraph was located with the floating find toolbar (`Cmd+F`), then styled with the ribbon styles menu button and the `标题 4` gallery item.
- The sidebar Find and Replace panel falsely reported no matches for `CASE-COMBINED-H4-02`; the floating find toolbar reliably found the same anchor.
- The document was saved with `Cmd+S` after each H4 repair and again after the final repair.

## Final document observation

- Size: 15,715 bytes.
- SHA-256: `e9a93eb960e5ca5c510e7755a4605aef9438aff2d803382ec442dd21bd410363`.
- Word status bar observed 30 pages and 33,426 words.
- All 17 expected anchors (`H1-H4` four each plus `CASE-COMBINED-END`) occur exactly once.

## Verification

- `unzip -t`: all archive members passed.
- `xmllint --noout word/document.xml`: passed.
- `word/document.xml` pStyle counts: style `1` x4, style `2` x4, style `3` x4, style `4` x4.
- Rust driver checks for `CASE-COMBINED-H1-01`, `CASE-COMBINED-H4-04`, and `CASE-COMBINED-END`: `contains_marker=true`, `no_edit_save_byte_identical=true`.
