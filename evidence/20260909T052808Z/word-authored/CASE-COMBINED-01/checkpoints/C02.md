# CASE-COMBINED-01 C02 checkpoint

## Word UI action

- Continued from `CASE-COMBINED-01-C01.docx` in the same Word document window.
- Created five structurally distinct tables through Word's table UI:
  1. `TABLE-01`: basic 3x3 grid.
  2. `TABLE-02`: 3x3 grid with the first column vertically merged (`V-Merge-Top`, one restart and two continue cells).
  3. `TABLE-03`: 2x2 grid with a numbered in-cell list and a pasted PNG image in the adjacent cell.
  4. `TABLE-04`: 4x2 grid whose first row is set to repeat as a header row.
  5. `TABLE-05`: 2x6 wide grid.
- Completed the Unicode and direct-formatting stress content:
  - CJK/Latin mixed text, emoji surrogate pair, combining mark, Devanagari sign, Arabic `مرحبا`, Hebrew, tab, U+00A0 NBSP, manual line break, and symbol text.
  - The `plain`, `bold`, `italic`, `underline`, and `clear` words are separate direct-formatted runs; `bold`, `italic`, and `underline` have bold, italic, and single-underline OOXML properties respectively.
- Saved twice with `Cmd+S` after each edit batch.

## Final package observation

- Size: 40,811 bytes.
- SHA-256: `c0a0a88ec8013f1c15ecd3f2118efc0f947b0b308c1f9817e6e9014df24ac207`.
- `unzip -t`: all archive members passed.
- `xmllint --noout word/document.xml`: passed.
- Persistent structure counts in `word/document.xml`: 5 tables, 3 `w:vMerge` tags, 1 `w:tblHeader`, 1 `w:drawing`, 1 tab, 1 manual line break, 1 bold tag, 1 italic tag, 1 underline tag, 1 NBSP, 1 emoji, 1 Arabic phrase, and 1 Hebrew phrase.
- Visual evidence: `C02-word-ui.png`, `C02-word-ui-table03.png`, `C02-word-ui-table04.png`, `C02-word-ui-table05.png`, and `C02-word-ui-unicode.png`.

## Rust driver

- `CASE-COMBINED-END`: `contains_marker=true`, `no_edit_save_byte_identical=true`.
- `CASE-COMBINED-H1-01`: `contains_marker=true`, `no_edit_save_byte_identical=true`.
- `CASE-COMBINED-UNICODE-01`: `contains_marker=true`, `no_edit_save_byte_identical=true`.
- Table and comment projections remain outside the current visible body projection, so table interiors are not asserted through the Rust reader.

## Automation note

- A first keyboard formatting attempt omitted the command modifier and temporarily replaced several formatting words. Those changes were undone through Word's Edit menu before redoing the edits with explicit `Cmd+B`, `Cmd+I`, `Cmd+U`, and `Ctrl+Space` commands. The final package contains the intended original words and formatting.
