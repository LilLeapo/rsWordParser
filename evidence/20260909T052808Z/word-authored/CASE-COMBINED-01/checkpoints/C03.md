# CASE-COMBINED-01 C03 checkpoint

## Word UI action

- Continued from `CASE-COMBINED-01-C02.docx` in the existing `CASE-COMBINED-01-C03` Word window.
- Created the C03 structure through Word UI:
  - Section 1 remains the default next-page portrait section.
  - Section 2 is a continuous portrait section.
  - Section 3 is the final landscape section (`16838 x 11906`, `w:orient="landscape"`).
- Inserted an automatic TOC for heading levels 1-3 (`TOC \o "1-3" \h \z \u`), producing 15 `PAGEREF` fields.
- Inserted a bookmark (`CASE_COMBINED_BOOKMARK`) and a `REF CASE_COMBINED_BOOKMARK \h` cross-reference.
- Inserted the external hyperlink `CASE-COMBINED-C03-EXTERNAL-LINK` to `https://example.com/rswordparser`; its relationship is `TargetMode="External"`.
- Added distinct headers and footers for all three sections:
  - `CASE-COMBINED-C03-HEADER-S1` / `CASE-COMBINED-C03-FOOTER-S1`
  - `CASE-COMBINED-C03-HEADER-S2` / `CASE-COMBINED-C03-FOOTER-S2`
  - `CASE-COMBINED-C03-HEADER-S3` / `CASE-COMBINED-C03-FOOTER-S3`
- Disabled Link to Previous for sections 2 and 3 before entering their header/footer text.
- Inserted a native Word `PAGE` field into the section 3 footer through the Page Number dialog; Word displayed `字段 33` before the final save.
- Saved twice with `Cmd+S` and closed header/footer editing before the final screenshot.

## Final package observation

- Size: 56,225 bytes.
- SHA-256: `2f41fb60a9b5f675b1aeef0034636e0acd6a707a9f5b24f1a6a49f564a5c3531`.
- `unzip -t`: all archive members passed.
- `xmllint --noout`: `word/document.xml` and all header/footer parts passed.
- `word/document.xml` contains 3 `w:sectPr` elements:
  - section 1: default/next-page, `11906 x 16838`, portrait
  - section 2: `continuous`, `11906 x 16838`, portrait
  - section 3: default/next-page, `16838 x 11906`, landscape
- The three default header targets and three default footer targets are distinct and resolve to the intended marker text.
- Section 3 footer `word/footer5.xml` contains `PAGE` in `w:instrText`.
- TOC field count: 1; `PAGEREF` field count: 15; bookmark and `REF` cross-reference both persist.
- Persistent C03 anchors: `CASE-COMBINED-C03-START`, `CASE-COMBINED-C03-HEADING-ONE`, `CASE-COMBINED-C03-HEADING-TWO`, `CASE-COMBINED-C03-BOOKMARK-TARGET`, `CASE-COMBINED-C03-EXTERNAL-LINK`, `CASE-COMBINED-C03-CROSS-REFERENCE`, `CASE-COMBINED-C03-TOC`, and `CASE-COMBINED-C03-SECTION3`.
- Independent XML evidence: `xml-check-C03.txt`.
- Visual evidence: `C03-sections-step1.png`, `C03-word-ui-toc.png`, `C03-crossref-dialog-before.png`, `C03-section3-inserted.png`, `C03-section3-landscape.png`, `C03-headers-footers.png`, and `C03-final.png`.

## Rust driver

All of the following returned `contains_marker=true` and `no_edit_save_byte_identical=true`:

- `CASE-COMBINED-C03-START`
- `CASE-COMBINED-C03-HEADING-ONE`
- `CASE-COMBINED-C03-HEADING-TWO`
- `CASE-COMBINED-C03-BOOKMARK-TARGET`
- `CASE-COMBINED-C03-EXTERNAL-LINK`
- `CASE-COMBINED-C03-CROSS-REFERENCE`
- `CASE-COMBINED-C03-TOC`
- `CASE-COMBINED-C03-SECTION3`

Driver output: `C03-driver.txt`.

## Verification gates

- Strict clippy was fixed in commit `611e10a` (`fix: satisfy strict clippy`).
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `cargo test --workspace --locked`: passed.
- C03 Word UI work began only after both gates passed.

## Automation notes

- AX indexes were re-read before every click because the Word ribbon and document tree indexes shifted during section and header/footer operations.
- The native Page Number dialog was used instead of typing a page number; AX showed the footer as `字段 33`, and XML independently confirmed a `PAGE` field.
- Header/footer text was entered only after Link to Previous was explicitly off for sections 2 and 3.
