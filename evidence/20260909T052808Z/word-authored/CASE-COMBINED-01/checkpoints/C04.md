# CASE-COMBINED-01 C04 checkpoint

## Word UI action

- Continued from `CASE-COMBINED-01-C03.docx` in the existing
  `CASE-COMBINED-01-C04` Word window.
- Enabled Track Changes and performed a multi-round edit batch:
  - Inserted `CASE-COMBINED-C04-PARA-01`,
    `CASE-COMBINED-C04-REV-INSERT-01`, and
    `CASE-COMBINED-C04-REV-INSERT-02` after the C03 start anchor.
  - Deleted two C03 phrases and inserted
    `CASE-COMBINED-C04-REV-REPLACED-01` and
    `CASE-COMBINED-C04-REV-REPLACED-02` as tracked replacements.
  - Applied bold, italic, and underline to
    `CASE-COMBINED-C04-REV-INSERT-02`. The attempted clear-formatting step did
    not remove those direct properties; the final run retains `w:b`, `w:i`,
    and `w:u` plus `w:rPrChange`.
  - Inserted Heading 2 paragraph `CASE-COMBINED-C04-TOC-ENTRY`, followed by a
    tab and `CASE-COMBINED-C04-TAB-01`, then a manual break and
    `CASE-COMBINED-C04-BREAK-01`.
  - Applied bold to the break marker. The attempted local clear-formatting
    step likewise left `w:b` with an `w:rPrChange`, so the actual Word outcome
    is recorded rather than the planned "formatting removed" outcome.
  - Added two Word comments. Initial empty comment ranges were corrected by
    focusing the actual comment-card editors, pasting the marker text, and
    saving twice.
- Saved twice with `Cmd+S` after the comment edits.

## Final package observation

- Size: 58,472 bytes.
- SHA-256: `cb93aacb84eec61df540309fca629000758704b3bbc15039c3be86aadce84987`.
- `unzip -t`: all archive members passed.
- `xmllint --noout`: all 29 XML/relationship parts passed.
- `word/document.xml` structure counts:
  - 10 `w:ins`, 2 `w:del`, 2 `w:delText`.
  - 2 `commentRangeStart`, 2 `commentRangeEnd`, 2 `commentReference`.
  - 32 `w:tab`, 2 `w:br`, 3 `w:sectPr`, 5 `w:tbl`, 1 `w:drawing`.
  - 1 TOC field and 15 `PAGEREF` fields.
  - 16 bookmark starts, 16 hyperlink elements, and 1 `REF` field.
  - 2 `PAGE` fields in retained header/footer parts.
- `word/comments.xml` contains comment IDs `20` and `26` with:
  - `CASE-COMBINED-C04-COMMENT-01`
  - `CASE-COMBINED-C04-COMMENT-02`
- All C04 anchors are present exactly once in `word/document.xml`:
  `PARA-01`, `REV-INSERT-01`, `REV-INSERT-02`, `REV-REPLACED-01`,
  `REV-REPLACED-02`, `TOC-ENTRY`, `TAB-01`, and `BREAK-01`.
- C03 bookmark, external hyperlink, cross-reference, TOC, three header
  markers, and three footer markers remain present.
- Independent XML evidence: `xml-check-C04.txt`.
- Visual evidence: `C04-comments-final.png`.

## Rust driver

All of the following returned `contains_marker=true` and
`no_edit_save_byte_identical=true`:

- `CASE-COMBINED-C04-PARA-01`
- `CASE-COMBINED-C04-REV-INSERT-01`
- `CASE-COMBINED-C04-REV-INSERT-02`
- `CASE-COMBINED-C04-REV-REPLACED-01`
- `CASE-COMBINED-C04-REV-REPLACED-02`
- `CASE-COMBINED-C04-TOC-ENTRY`
- `CASE-COMBINED-C04-BREAK-01`

Driver output: `C04-driver.txt`.

## Automation notes

- AX indexes changed repeatedly as the comment pane and revision cards
  re-rendered. Each click was preceded by a fresh AX read; dynamic index
  extraction was required for the comment-card editors.
- `typeText` did not enter text into the modern comment-card editor. Pasting
  into the focused card worked, and the text persisted after two `Cmd+S`
  saves.
- `Cmd+Return` was not required for persistence in this Word version. The
  comment text was confirmed only through `word/comments.xml`, not UI alone.
- The two clear-direct-formatting attempts are recorded as an observed
  discrepancy: the final OOXML retains direct formatting and adds
  `w:rPrChange` revisions instead of removing the properties.
