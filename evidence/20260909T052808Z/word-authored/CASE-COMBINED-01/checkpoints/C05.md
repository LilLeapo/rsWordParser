# CASE-COMBINED-01 C05 checkpoint

## Word UI action

- Closed and reopened the archived `CASE-COMBINED-01-C04.docx` in Word
  16.112.3.
- Used `References > Update Table > Update entire table` while Track Changes
  remained enabled.
- Saved twice with `Cmd+S`, then used Word Save As to create
  `CASE-COMBINED-01-C05.docx`.
- Closed and reopened C05 and verified the persisted document before final
  archival. Only the CASE-COMBINED-01 C05 window was operated.
- The temporary TOC-updated working copy of C04 had SHA-256
  `d99159ce749bc7001c5050b1c3d31f9fe39d891a1e98a3ca2784735fbcb8db96`.
  The tracked C04 file was restored to its committed SHA-256
  `cb93aacb84eec61df540309fca629000758704b3bbc15039c3be86aadce84987`;
  that temporary hash is not C04 evidence.

## Close/reopen observation

- Word displayed 34 pages. Before the final close the status bar reported
  33,581 words; after reopening it reported 42,368 characters (approximate).
- The updated TOC cache displayed
  `CASE-COMBINED-C04-TOC-ENTRY ... CASE-COMBINED-C04-TAB-01
  CASE-COMBINED-C04-BREAK-01 ... 32`.
- Both comments, tracked revisions, bookmarks, the external hyperlink,
  cross-reference, three section headers, and three section footers remained
  visible after reopen.

## Final package observation

- Size: 59,924 bytes.
- SHA-256: `459a4016749e178503cfd9d7a6e976ec90c9c9cb98802cd3775669eace406a84`.
- `unzip -t`: all archive members passed.
- `xmllint --noout`: all 29 XML/relationship parts passed.
- `word/document.xml` structure counts:
  - 42 `w:ins`, 32 `w:del`, 32 `w:delText`.
  - 2 comment ranges/references, 66 tabs, 2 manual breaks, 3 sections,
    5 tables, and 1 drawing.
  - 1 TOC field, 16 `PAGEREF` fields, 17 bookmark starts, 1 external
    hyperlink element, and 1 `REF` field.
  - 2 `PAGE` fields in retained header/footer parts.
- TOC update persistence:
  - `CASE-COMBINED-C04-TOC-ENTRY` appears twice: once in the body and once in
    the updated TOC cache.
  - `CASE-COMBINED-C04-TAB-01` and `CASE-COMBINED-C04-BREAK-01` each appear
    twice for the same reason.
  - `PAGEREF` increased from 15 in C04 to 16 in C05.
- `word/comments.xml` contains IDs `20` and `27` with
  `CASE-COMBINED-C04-COMMENT-01` and `CASE-COMBINED-C04-COMMENT-02`.
- C03 bookmark/`REF`, external hyperlink relationship, TOC marker, three
  header markers, three footer markers, three default header references, three
  default footer references, and one landscape section remain present.
- Independent XML evidence: `xml-check-C05.txt`.
- Visual evidence: `C05-final.png`.

## Rust driver

All 16 checks returned `contains_marker=true` and
`no_edit_save_byte_identical=true`:

- C04: `PARA-01`, `REV-INSERT-01`, `REV-INSERT-02`, `REV-REPLACED-01`,
  `REV-REPLACED-02`, `TOC-ENTRY`, `TAB-01`, `BREAK-01`.
- C03: `START`, `HEADING-ONE`, `HEADING-TWO`, `BOOKMARK-TARGET`,
  `EXTERNAL-LINK`, `CROSS-REFERENCE`, `TOC`, `SECTION3`.

Driver output: `C05-driver.txt`.

## Actual-outcome notes

- Updating the TOC with Track Changes enabled introduced a large set of
  tracked field-result deletions and insertions. The C04 structure counts
  therefore were not required to remain numerically unchanged.
- Word rewrote the TOC field result and reduced direct `w:hyperlink` elements
  from 16 in C04 to 1 in C05 (the external link). The TOC entries still carry
  `HYPERLINK` field instructions and `PAGEREF` fields in the updated cache.
- The clear-direct-formatting discrepancy recorded in C04 remains part of the
  source document and was not rewritten as a pass.
