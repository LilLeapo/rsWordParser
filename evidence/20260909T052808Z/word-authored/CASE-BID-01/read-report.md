# CASE-BID-01 read report

## Word creation

The document was created in Microsoft Word 16.112.3 through desktop UI actions:
typing/Word paste for synthetic text, Heading 1/2 styles, numbered list, Word
Table dialog, Table > Merge Cells, Word picture picker, section break, page
orientation, Track Changes, and comment insertion. The 120x120 PNG was a local
test asset selected in Word; no DOCX was generated by script.

## C01

- XML: 4 Heading 1 paragraphs, 1 Heading 2 paragraph, one numbering definition,
  and stable anchors `CASE-BID-00`, `CASE-BID-SCOPE`, and `CASE-BID-END`.
- Rust: `CASE-BID-END` visible; no-edit save byte-identical.
- SHA-256: `985b308930decee630921add2a54b5861c63cfbc95179cfd4fd19166a7f122ac`.

## C02

- XML: 2 `<w:tbl>` elements, one horizontal merge (`gridSpan`), one drawing, and
  that drawing is inside a table (`count(//tbl//drawing)=1`). The media part is
  `word/media/image1.png`. Numbered list remains present.
- Rust: `CASE-BID-END` visible; no-edit save byte-identical.
- SHA-256: `f54a48fcc3b7c2f748248dde40da74f0f26db4d13d4f02581a876a063b69c344`.

## C03

- XML: 2 sections, one landscape page (`16838x11906`, `orient=landscape`), and
  3 tables. `CASE-BID-LANDSCAPE-SECTION` and `CASE-BID-LANDSCAPE-TABLE` are
  present in table content.
- Rust: package opens and no-edit save is byte-identical, but table-internal
  anchors are not visible through `Document::text_blocks()`.
- Classification: package fidelity `PASS`; table/body semantic projection
  `NOT_IMPLEMENTED` in the current M1 model.
- SHA-256: `e8bfe413928f16aad52ffb5eb4dd613ec8f1dbd8ad5aeb2998f9ae69d3346f4a`.

## C04

- Word UI: enabled Track Changes, added ` CASE-BID-REV-01` after
  `CASE-BID-END`, and attached a comment containing `CASE-BID-COMMENT-END`.
- XML: 2 `w:ins` elements and one comment range; comments.xml contains the
  comment marker.
- Rust: `CASE-BID-REV-01` visible because the revision is in body text; no-edit
  save byte-identical.
- SHA-256: `1814208818761a5f0396489233fca1579f46fab9d11391b896532aab3bd6d9b1`.

## C05

- Word close/reopen found `CASE-BID-REV-01` on page 3 and
  `CASE-BID-COMMENT-END`.
- Word Save As produced the independent final file.
- Final XML: 2 sections, one landscape section, 3 tables, one in-table drawing,
  2 `w:ins`, one comment range, and all stable markers.
- Final Rust read: `CASE-BID-REV-01` visible; no-edit save byte-identical.
- SHA-256: `e94ccd1743f3b7351a1907c7e573ba48c3e76f9379885bf84148432d33206733`.

## Limitations

- The current Rust driver proves package open, model marker visibility, and
  no-edit byte fidelity. It does not yet produce a semantic table projection.
- Table content and image-in-cell semantics are verified by independent XML and
  zip inspection, not by current `Document::rebuild`.
