# Chart Derived Results

Visible chart reread: 136 documents. All 26 chartdata documents opened chart data. Existing Word readings show value changes after activation in 23 documents; exact comparisons to recorded baseline values confirm 23 rollbacks, 3 unchanged baseline values, and 0 pending comparison(s).

The bubble chart and two scatter charts already expose 10/20/30 before editing data. The no-title chart still has an empty title. These expectation mismatches are retained.

In 11 ChartEx-base documents, the added chart and shape-count tests pass. Overall incomplete readings refer to the pre-existing ChartEx series-values limitation; its raw diagnostics are retained.
