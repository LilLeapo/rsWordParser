//! 有效属性只读视图（`spec/07-resolve.md`，`docs/03` §7）。
//!
//! 样式链、toggle、主题字体/颜色、Cs 选择、编号标记、节继承、表格条件格式。每个值带来源
//! （`RES-01`）。toggle（`RES-04`）不采用 child-overrides-parent，以 Word 实测 fixture 为准。
//! M1 任务 1.9 为首版（toggle 用占位规则并标注）。
